//  shift and unshift in js

// shift 

// unshift
var arr = [1,2,3,4,5,6,7];
arr.unshift(0); // unshift is the method to add value in the 0th index of array
console.log(arr);

var arr = [1,2,3,4,5,6,7];
arr.shift(0); // shift is the method to delet value in the 0th index of array
console.log(arr);